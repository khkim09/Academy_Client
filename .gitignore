# 의존성 폴더 (라이브러리)
node_modules/

# 프로덕션 빌드 폴더
# `npm run build` 실행 시 생성되는 최종 결과물 폴더입니다.
# 용량이 매우 크고, 소스 코드가 아니므로 GitHub에 올리지 않습니다.
build/

# 환경 변수 파일
# 로컬 개발 시 사용하는 API 키 등을 포함합니다.
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# 로그 파일
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# 테스트 리포트
coverage/

# 운영체제 및 에디터 관련 파일
.DS_Store
Thumbs.db
.vscode/
.idea/
